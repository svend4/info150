"""Optional Web UI surface for triage4-fish.

Entry point: ``triage4_fish.ui.dashboard_api:app`` (FastAPI).
Pulls in fastapi + uvicorn via the ``[ui]`` extra; the rest of the
package does not depend on either.
"""
