"""Optional Web UI surface for triage4-aqua.

Entry point: ``triage4_aqua.ui.dashboard_api:app`` (FastAPI).
Pulls in fastapi + uvicorn via the ``[ui]`` extra; the rest of the
package does not depend on either.
"""
